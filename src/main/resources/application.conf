
joel {
  hostname = "localhost"
  port = 10001


  akka {
    loggers = ["akka.event.Logging$DefaultLogger"]
    loglevel = "INFO"

    actor {
      provider = "akka.cluster.ClusterActorRefProvider"
    }
    remote {
      enabled-transports = ["akka.remote.netty.tcp"]
      netty.tcp {
        hostname = "localhost"
        port = 30001
      }
    }

    cluster {
      seed-nodes = ["akka.tcp://joel@localhost:30001"]
      auto-down-unreachable-after = 10s
    }
  }
}

yang {
  hostname = "localhost"
  port = 10002

  akka {
    loggers = ["akka.event.Logging$DefaultLogger"]
    loglevel = "INFO"

    actor {
      provider = "akka.cluster.ClusterActorRefProvider"
    }
    remote {
      enabled-transports = ["akka.remote.netty.tcp"]
      netty.tcp {
        hostname = "localhost"
        port = 30002
      }
    }

    cluster {
      seed-nodes = ["akka.tcp://joel@localhost:30001"]
      auto-down-unreachable-after = 10s
    }
  }

}

